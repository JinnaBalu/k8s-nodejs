apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-sample-app
spec:
  selector:
    matchLabels:
      app: nodejs-sample-app
  replicas: 1
  template:
    metadata:
      labels:
        app: nodejs-sample-app
    spec:
      containers:
      - name: nodejs-sample-app
        image: jinnabalu/nodejs-sample-app
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-sample-app
spec:
  selector:
    app: nodejs-sample-app
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 3000
    nodePort: 30006
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodejs-sample-app
spec:
  rules:
  - host: example.com
    http:
      paths:
      - path: /nodejs-sample-app
        pathType: Prefix
        backend:
          service:
            name: nodejs-sample-app
            port:
              name: http
